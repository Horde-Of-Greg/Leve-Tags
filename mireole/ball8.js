
util.executeTag("has_krill_issue");
(() => {
  const n = Math.floor(Math.random() * 8);
  const results = ["it is certain", "without a doubt", "most likely", "yes", "ask again later", "concentrate and ask again", "no", "very doubtful"];
  return (results[n]);
})()
